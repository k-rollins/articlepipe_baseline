    <% if current_user %>
      logged in as <%= current_user.display_name %>  | <%= link_to("Log out", destroy_user_session_path, :method => :delete) %> | <%= link_to("My Profile", "/profiles/#{current_user.id}")%>(<%= link_to("edit", "/profiles/#{current_user.id}/edit")%>)
    <% else %>
      <%= link_to("Sign in", new_user_session_path) %> | <%= link_to("Create Account", new_user_registration_path) %>
    <% end %>  